

const active = true;

// let message = '';

// if(active) {
//     message = 'Active';
// } else {
//     message = 'Disable';
// }

// const message = (active) ? 'Active' : 'Disable';

const message = !active && 'Active';

console.log(message);
